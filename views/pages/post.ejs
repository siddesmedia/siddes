<div class="postdiv">
    <% if(moderator == true) { if(post.approved == true) { %>
    <button onclick="removepost('<%= post._id %>')" type="button">Mod: Remove Post</button>
    <% } else { %>
    <button onclick=" removepost('<%= post._id %>')" type="button">Mod: Remove Post</button>
    <button onclick=" approvepost('<%= post._id %>')" type="button">Mod: Approve Post</button>

    <% }} %>
    <% if (post.approved == true) { %>
    <span class="commentsmeta right">
        <button type="button" class="postreport">Approved</button>
    </span>
    <% } else { if (post.reported == true) { %>
    <span class="commentsmeta right">
        <button type="button" class="postreport">Under Review</button>
    </span>
    <% } else { %>
    <span class="commentsmeta right">
        <button onclick="window.location='/report?post=<%= post._id %>'" type="button" class="postreport"><a
                class="nostyle" href="/report?post=<%= post._id %>">Report</a></button>
    </span>
    <% }} %>
    <% if(owner.displayname == "") { %>
    <h2>
        <a href="/<%= owner.username %>"><%= owner.displayname %></a>
        <% if(post.repost == 'true') { %>
        <span class="repostbadge">Repost</span>
        <% } else {} %>
    </h2>
    <h3>@<a href="/<%= owner.username %>"><%= owner.username %></a>
    </h3>
    <% } else { %>
    <h2>@<a href="/<%= owner.username %>"><%= owner.username %></a>
    </h2>
    <% } %>
    <div>
        <p class="postbody" id="postbody">
            <%= post.body.toString() %>
        </p>
    </div>
    <button onclick="window.location='/account/new?text=<%- encodeURIComponent(post.body) %>&repost=true'" type="button"
        class="postrepost">Repost</button>
    <% if(liked == true) { %>
    <form action="/like/remove" method="POST" class="right">
        <input name="postid" class="hidden" value="<%= post._id %>">
        <button class="postlike red" onclick="like('<%= post._id %>')">
            <!--♥️-->Unlike -
            <%= post.likes.length %></button>
    </form>
    <% } else { %>
    <form action="/like/new" method="POST" class="right">
        <input name="postid" class="hidden" value="<%= post._id %>">
        <button class="postlike" onclick="like('<%= post._id %>')">
            <!--♥️-->Like -
            <%= post.likes.length %></button>
    </form>
    <% } %>
    <button><%= post.date.toLocaleString() %></button>
</div>
<h2 class="header" id="comments">Comments:</h2>
<div class="postdiv">
    <form action="/comment/new" method="POST">
        <input name="body" type="text" style="width:30%" autocomplete="off" class="postcommentinput" required="required"
            placeholder="Thoughts...">
        <input name="parentid" type="text" style="width:0%" class="hidden" value="<%= post._id %>" required="required">
        <button type="submit" class="postcomment">Post Comment</button>
    </form>
</div>
<br>
<div>
    <% if(comments.length == 0) { %>
    <p class="headerred">There are no comments :(</p>
    <% } else { %>
    <% for(var i = 0; i < comments.length; i++) { %>
    <div class="commentcontainer">
        <div class="postdiv">
            <p class="commentsowner">
                <a class="postownerlink" href="/account/<%= comments[i].owner %>"
                    id="comment_<%= i %>_<%= comments[i].owner %>">View Poster</a>
            </p>
            <p class="commentsbody"><%= comments[i].body %></p>
            <button><%= comments[i].date.toLocaleString() %></button>
        </div>
    </div>
    <script>
        getusername("comment_<%= i %>_<%= comments[i].owner %>", "<%= comments[i].owner %>")
    </script>
    <% } %>
    <% } %>
</div>
<script>
    replacepostlinks('postbody')
</script>